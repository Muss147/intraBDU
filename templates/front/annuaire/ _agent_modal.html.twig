<template>
    <div id="agent-modal" class="fixed top-0 left-0 right-0 z-50 w-full p-4 overflow-y-auto md:inset-0 h-[calc(100%-1rem)] max-h-full">
        <div class="relative w-full max-w-md max-h-full bg-white rounded-lg shadow-sm">
            <div class="flex flex-col items-center pb-10">
                <img class="w-24 h-24 mb-3 rounded-full shadow-lg" src="{{ asset(agent.photo.filename|default('assets/media/svg/avatars/blank.svg')) }}" alt="">
                <h5 class="mb-1 text-xl font-medium text-gray-900">{{ agent.nom }} {{ agent.prenoms }}</h5>
                <span class="text-sm text-gray-500">{{ agent.fonction }}</span>
                <div class="flex mt-4 md:mt-6">
                    <a href="#" class="btn btn-primary">Appeler</a>
                    <a href="mailto:{{ agent.email }}" class="btn btn-light ms-2">Email</a>
                </div>
            </div>
        </div>
    </div>
</template>